Feat: Multi-Scenario Support & Frontend Refactoring (Phase 2)

Features:
- **Multi-Scenario Support**: Engine now iterates through list of scenarios (Frequency+Severity pairs) and aggregates annual losses.
- **Schema Update**: Added `models` list to `Severity` object in `crml_model.py`.
- **Frontend Components**: Refactored `page.tsx` into `Hero`, `Features`, `UseCases`, `QuickStart`, `CTA`.
- **Validator UI**: Split `ValidationResults.tsx` into `ValidationSummary`, `ModelMetadataCard`, `ErrorDetailList`.

Bug Fixes:
- **Environment**: Fixed `crml` validation error by enforcing local `.venv` path in CLI calls.
- **Currency**: Fixed `TypeError: can't multiply sequence` by enforcing numpy arrays in `engine.py`.
- **Mixture Model**: Fixed crash when parsing string values (e.g. "175 000") in mixture components.
- **FXConfig**: Fixed validation error on default rates.

Tests:
- Added `tests/test_multi_scenario.py`.
- Verified regression suite (49 pass).
- Added `spec/examples/multi-scenario-example.yaml`.
